<?xml version="1.0" encoding="utf-8"?>
<queries>
  <!-- Example SQL queries for the Barbershop backend -->
  <query id="GetAllShops">
    <sql>
      SELECT 
        id
        ,name
        ,address 
        ,language 
        ,created_at 
        ,updated_at
      FROM 
        shops;
    </sql>
  </query>

  <!-- Insert customer-->
  <query id="InsertCustomer">
    <sql>
      INSERT INTO customers
    (
      name,
      phone
    )
    VALUES
    (
      @name,
      @phone
    )
    RETURNING id;
    </sql>
  </query>

  
  <!-- Insert visits-->
  <query id="InsertVisits">
    <sql>
      INSERT INTO visits
    (
      customer_id,
      shop_id,
      barber_id
    )
    VALUES
    (
      @customer_id,
      @shop_id,
      @barber_id
    )
    RETURNING id;
    </sql>
  </query>

    <!-- Insert queue-->
  <query id="InsertQueue">
    <sql>
      INSERT INTO queue
    (
      visit_id,
      shop_id,
    )
    VALUES
    (
      @visit_id,
      @shop_id,
    )
    RETURNING id;
    </sql>
  </query>

</queries>

